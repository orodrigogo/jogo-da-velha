import React from "react";
import "./style.css";
import X from "../../objects/player-item";

const SectionHash = () => (
  <section className="section-hash">
    <X />
  </section>
);

export default SectionHash;
